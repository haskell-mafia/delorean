#!/bin/sh -exu

$(dirname $0)/ci.branches

VERSION=$(date '+%Y%m%d%H%M%S')-$(git log --pretty=format:%h -n 1)

cabal haddock --hoogle
aws s3 --region=ap-southeast-2 cp --recursive dist/doc/html/delorean/ s3://ambiata-doc/delorean-haddock/$VERSION
